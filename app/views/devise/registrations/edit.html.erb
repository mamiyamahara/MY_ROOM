<div class="container">
  <div class="row">
    <div class= "col-lg-4"></div>

    <div class= "col-lg-4">
      <h2 style= "margin-bottom: 30px;">パスワード変更画面</h2>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>


          <div class="field" style= "margin-bottom: 30px;">
            <%= f.label :メールアドレス %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
          </div>

          <% if devise_mapping.confirmable? && resource.pending_rion? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>

          <div class="field" style= "margin-bottom: 30px;">
            <%= f.label :現在のパスワード %> <br />
            <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control" %>
          </div>

          <div class="field" style= "margin-bottom: 30px;">
            <%= f.label :新しいパスワード（6文字以上） %> <br />
            <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
          </div>

          <div class="field" style= "margin-bottom: 30px;">
            <%= f.label :新しいパスワード（確認用） %><br />
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
          </div>

          <div class="actions" style= "margin-bottom: 30px;">
            <%= link_to "戻る", :back, class: "btn btn-dark" %>
            <%= f.submit "変更", class: "btn btn-dark" %>
          </div>
        <% end %>

      <div class= "col-lg-4"></div>
    </div>
  </div>
</div>


<h3>アカウントの削除</h3>

<p>アカウントを削除すると、過去の投稿やユーザー情報など、ご利用いただいたサービスの一切が消去されます。<br>一度削除したアカウントの復元はできません。 <%= button_to "アカウントを削除する", registration_path(resource_name), data: { confirm: "本当に削除しますか？" }, method: :delete , class: "btn btn-danger" %></p>


